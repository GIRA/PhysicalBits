var trigPin;
var echoPin;
var maxDistance = 100;

proc init(trig, echo, maxDist) {
  trigPin = trig;
  echoPin = echo;
  maxDistance = maxDist;
}

func distanceCm() {
  return sonarDistCm(trigPin, echoPin, maxDistance);
}
