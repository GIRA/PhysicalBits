var debounceMs = 50;

proc waitForRelease(pin) {
	until isOn(pin);
	delayMs(debounceMs);
	until isOff(pin);
	delayMs(debounceMs);
}


proc waitForPress(pin) {
	until isOff(pin);
	delayMs(debounceMs);
	until isOn(pin);
	delayMs(debounceMs);
}
