initializing
initializeWithImports: importsArray variableDefinitions: varsArray scripts: scriptsArray 
	imports := importsArray.
	variableDefinitions := varsArray.
	scripts := scriptsArray.
	self initialize
	